# Hasura GraphQL endpoint URL
# For production on GitHub Pages, use your Hasura Cloud URL
# Replace with your actual Hasura endpoint
NEXT_PUBLIC_HASURA_URL=https://your-hasura-project.hasura.app/v1/graphql

# Hasura admin secret (keep this secret!)
# For production, use GitHub Secrets instead of committing this value
NEXT_PUBLIC_HASURA_ADMIN_SECRET=your-hasura-admin-secret-here
